# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'QuizWorldAPI' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod "Argo"
  pod 'ReactiveCocoa', '~> 5.0.3'
  pod 'ReactiveExtensions'
  pod 'Prelude'
  pod 'Curry'

  # Pods for QuizWorldAPI

  target 'QuizWorldAPITests' do
    inherit! :search_paths
    
    pod 'ReactiveCocoa', '~> 5.0.3'
    pod "Argo"
    pod 'ReactiveExtensions'
    pod 'Prelude'
    pod 'Curry'
    # Pods for testing
  end

end
